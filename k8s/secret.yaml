# TEMPLATE — never commit real values.
# Generate and apply manually:
#   kubectl create secret generic aceest-secret \
#     --from-literal=SECRET_KEY=<your-strong-random-key> \
#     -n aceest-fitness
apiVersion: v1
kind: Secret
metadata:
  name: aceest-secret
  namespace: aceest-fitness
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_WITH_STRONG_RANDOM_VALUE"
