---
# Production Service (serves real users)
apiVersion: v1
kind: Service
metadata:
  name: aceest-production-svc
  namespace: aceest-fitness
  labels:
    app: aceest-fitness
    track: production
spec:
  selector:
    app: aceest-fitness
    track: production
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: NodePort
---
# Shadow Service (receives mirrored traffic — responses discarded)
apiVersion: v1
kind: Service
metadata:
  name: aceest-shadow-svc
  namespace: aceest-fitness
  labels:
    app: aceest-fitness
    track: shadow
spec:
  selector:
    app: aceest-fitness
    track: shadow
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: NodePort
---
# ============================================================
# Shadow Ingress — NGINX mirror annotation
# Every request to production is also sent to shadow-svc.
# Mirror responses are silently dropped.
# ============================================================
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aceest-shadow-ingress
  namespace: aceest-fitness
  annotations:
    nginx.ingress.kubernetes.io/mirror-uri: "/"
    nginx.ingress.kubernetes.io/mirror-host: "aceest-shadow-svc.aceest-fitness.svc.cluster.local"
    # Note: configuration-snippet disabled in this cluster (security policy)
    # Mirror traffic is handled via mirror-uri + mirror-host annotations above
spec:
  ingressClassName: nginx
  rules:
    - host: shadow.aceest.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: aceest-production-svc
                port:
                  number: 80
